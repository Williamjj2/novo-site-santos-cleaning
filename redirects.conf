# 🔄 REDIRECTS 301 - Santos Cleaning Solutions
# Arquivo de configuração para redirects do site antigo

# Redirect do domínio antigo (se houver)
# server {
#     listen 80;
#     server_name old-domain.com www.old-domain.com;
#     return 301 https://santoscsolutions.com$request_uri;
# }

# Redirects de páginas específicas do site antigo
# Adicione conforme necessário

# Exemplo de redirects comuns para empresas de limpeza:
rewrite ^/home/?$ / permanent;
rewrite ^/index\.html?$ / permanent;
rewrite ^/services\.html?$ /services permanent;
rewrite ^/contact\.html?$ /contact permanent;
rewrite ^/about\.html?$ /about permanent;
rewrite ^/cleaning-services/?$ /services permanent;
rewrite ^/house-cleaning/?$ /services permanent;
rewrite ^/office-cleaning/?$ /services permanent;
rewrite ^/pricing/?$ /services permanent;
rewrite ^/estimates/?$ /contact permanent;
rewrite ^/quote/?$ /contact permanent;
rewrite ^/service-areas/?$ /areas permanent;
rewrite ^/locations/?$ /areas permanent;
rewrite ^/marietta/?$ /areas/marietta permanent;

# Redirects para remoção de extensões
rewrite ^/(.+)\.php$ /$1 permanent;
rewrite ^/(.+)\.html$ /$1 permanent;

# Redirects para URLs com trailing slash
rewrite ^/([^/]+)/$ /$1 permanent;

# Redirects de imagens antigas (se necessário)
# rewrite ^/old-images/(.+)$ /images/$1 permanent;

# Redirects de blog posts (se existirem)
# rewrite ^/blog/(.+)$ https://blog.santoscsolutions.com/$1 permanent;

# Catch-all para páginas não encontradas do site antigo
# Redireciona para home com query parameter para tracking
# location ~* \.(php|asp|aspx|jsp)$ {
#     return 301 https://santoscsolutions.com/?migrated=1;
# } 